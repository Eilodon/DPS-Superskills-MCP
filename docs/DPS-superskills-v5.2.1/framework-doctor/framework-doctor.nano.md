# framework-doctor — nano

**Trigger:** Before releasing/installing/modifying Super Skills itself.

Run `python3 tools/framework_doctor.py .`, then `python3 tools/run_framework_fixtures.py .`. Verify YAML frontmatter, skill/nano match, README count, registry references, stale version refs, shell guard traps, shared references, tool syntax, fixtures, automation references, and `dps.py --help`. Use `python3 tools/epistemic_health_check.py .` for staleness checks.

Use claim grammar for the final self-audit claim.

→ Full: `framework-doctor/SKILL.md`
