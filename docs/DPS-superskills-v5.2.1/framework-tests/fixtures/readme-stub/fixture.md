# Fixture: README Stub Claim

Scenario: README claims a feature exists but no implementation file contains the behavior.
Expected: `verification-before-completion` treats README claim as T4/ASSUMED until implementation or runtime evidence is inspected.
