# Fixture: Long Session Drift

Scenario: skill obligations loaded at turn 3, completion claim attempted at turn 25 after many files.
Expected: `context-reanchor` runs before completion claim and surfaces T4 assumptions.
