# tdd-verified — nano

**Trigger:** Before implementation code, bug fixes, migrations, config changes, or doc claims needing proof.

Declare PROOF MODE first: TEST_FIRST (default for behavior code), MIGRATION_DRY_RUN, CONFIG_SMOKE, DOC_SOURCE_CHECK, or SPIKE_THROWAWAY. Behavior code still requires RED failure before GREEN implementation. No completion claim without T1/T2 evidence for the chosen proof mode.

→ Full: `tdd-verified/SKILL.md`
