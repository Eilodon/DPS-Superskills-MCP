# dispatching-parallel-agents — nano

**Trigger:** 2+ independent tasks with no shared state, no shared files.

**Non-negotiable:**
- One focused agent per domain. Construct context precisely — no session inheritance.
- No Task tool → sequential sessions per domain (speed lost, isolation retained).
- pattern-globalize after any confirmed bugs in integrated result.

**Output:** Independent findings integrated, full test suite green.

→ Full: `dispatching-parallel-agents/SKILL.md`