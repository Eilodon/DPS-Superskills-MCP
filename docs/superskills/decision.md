# Knowledge Base — decision

Auto-generated by kb_write. Do not edit header.

### KB-DECISION-t21-js-tool-inprocess-odra-backend
- **date:** 2026-06-24
- **source:** direct-observation
- **shareable:** false
- **content:** T2.1 composition MCP surface targets the IN-PROCESS Odra state model (Option B), not Pharos Solidity (A) nor a live casper-js-sdk client (C). Context: karma.tool.ts is hard-bound to the Pharos/Solidity KarmaService which has no composition; the composition primitive lives only on the Casper/Odra Rust contract (45/45 tests). Every existing Casper demo (demo_casper_e2e/composability) already models Odra in-process ("drop-in swappable for a live StdioMcpClient"). Decision: extend the in-process OdraStateMachine with register_composition/is_composite/get_composition + composite-aware settle (mirroring the Rust contract), expose via an MCP-shaped tool surface, vitest-covered. Consequences: delivers the Casper wow (composition via MCP) within the hackathon deadline without an audited-contract change; the Pharos Solidity port + real karma.tool.ts wiring stay P3 backlog. Rationale: A requires redeploying an audited contract (roadmap defers Casper-first); C needs live RPC + funded testnet + WASM deploy (sandbox can't). Owner-confirmed 2026-06-24.

### KB-DECISION-p3-hard-symmetric-dispute-bond-rfc
- **date:** 2026-06-24
- **source:** direct-observation
- **shareable:** false
- **content:** T0.3 RFC drafted (docs/rfc/2026-06-24-symmetric-dispute-bond.md) for P3-hard slashing. Chosen design: SYMMETRIC DISPUTE BONDS, loser-pays, layered on the existing optimistic REVIEW_WINDOW + pull-payment ledger (no new custody surface). disputeResult becomes bond-backed (B_d = max(MIN, bps*escrow)); provider matches B_p or concedes; arbitrate(verdict) by an Arbiter role (phase 1 = owner/multisig, phase 2 = Kleros swap, isolated behind the role). Provider reputation slashed ONLY on adjudicated ProviderAtFault or concede — safe because the disputer staked an equal bond (frivolous dispute is -EV: EV ~ -E for competent arbiter). Requires lifting PD-005 monotonic-up, so T0.5 native rep decay co-delivers. Status: DRAFT awaiting owner approval (DP-5 gate); T0.4 impl deferred post-hackathon (DP-1). Open questions OQ-1..OQ-4: disputeBondBps default, protocol sink for forfeited bond, ship ARBITER_VETO_DELAY in v1, REP_SLASH_STEP magnitude. Evaluated UMA (chosen, lightest), Kleros (phase-2 arbiter), Augur (fork backstop ref), EigenLayer (slashing safety rails).

### KB-DECISION-t13-rep-oracle-js-prover-existing-consumer
- **date:** 2026-06-25
- **source:** direct-observation
- **shareable:** false
- **content:** T1.3 cross-chain rep oracle: ship the off-chain PROVER as a pure JS service (src/lib/zk/rep_oracle.ts: aggregateObservations → ≤CIRCUIT_N distinct-category tuples with weighted-avg score/summed jobs/dominant provider/top-N-by-evidence; satisfiesThresholds pre-flight; buildRepAggInputs over an injected RepSource). The CONSUMER cache needs no new contract for v1 — reputation_aggregation_verifier already stores an agent-bound CredentialRecord keyed by per-epoch nullifier, which skills gate on by (epochRoot, threshold). demo_cross_chain.ts rewired through the oracle (was canned repHistory). Follow-ons (P3): live RepSource over karmaService.streamJobCompletedEvents + flow_reputation; Odra consumer for parity; address-keyed cache only if a non-private lookup is needed. 10 vitest cases; full suite 636 pass.

### KB-DECISION-mcp-resources-capability-and-kb-test-isolation
- **date:** 2026-06-26
- **source:** direct-observation
- **shareable:** false
- **content:** MCP skill resources must register the standard SDK `resources` capability before connect, not only raw `resources/*` handlers. Direct JSON-RPC showed `resources/list` and `resources/read` worked, but `initialize` advertised only `tools`, which made client resource discovery fragile. Decision: `registerSkillResources()` now calls `server.server.registerCapabilities({ resources: { listChanged: false } })`; regression test asserts capability negotiation. Knowledge-base tests must not mutate tracked `docs/superskills/*.md`; `MCP_KB_PATH` now redirects `kb_write`/`kb_query`/`kb_health`/`kb_update` and skill project-context injection to a temp KB root during tests. README now documents the actual surface: 16 MCP tools plus 31 read-only `skill://` resources. Evidence: lint exits 0, typecheck passes, build passes, and vitest reports 46 files / 380 tests passing. Next cycle trigger: when the MCP SDK public resource API changes or a client again reports resource discovery/listing mismatch.
